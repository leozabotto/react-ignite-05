{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "^1.13.4"
  }
}
